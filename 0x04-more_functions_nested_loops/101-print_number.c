#include "main.h"
void print_integer(int m);

/**
 * print_number - a function that prints an integer
 * @n: Integer
 * Return: 0
 */
void print_number(int n)
{
	unsigned int a;

	a =n;

	if (n < 0)
	{
		_putchar(45)
			a = -n;
	}
	if (x / 10)
		print_number(a /10);
	_putchar((a % 10) = '0');
}
